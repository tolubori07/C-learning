#include <math.h>
#include <stdio.h>

int main() {
  double a = sqrt(9);
  double b = pow(2, 32);
  int c = round(3.14);
  int d = ceil(3.14);
  int e = floor(3.99);
  double f = fabs(-100);
  double g = log(3);
  double h = sin(45);
  double i = cos(45);
  double j = tan(45);

  printf("\n%1f", f);
  return 0;
}
