#include <stdio.h>

int main() {

  // constant = fixed value that cannot be altered by the program during its
  // execution
  // convention for constants is to write them in ALL CAPS

  const float PI = 3.14159;

  // PI = 420.69; YOU CAN'T CHANGE THIS

  printf("%f", PI);

  return 0;
}
